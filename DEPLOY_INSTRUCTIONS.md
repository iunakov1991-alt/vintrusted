# Инструкция для деплоя через 12 часов

## Автоматический деплой

Я не могу автоматически выполнить деплой через 12 часов, так как работаю только в реальном времени. 

## Что нужно сделать:

### Вариант 1: Запустить скрипт вручную

Через 12 часов (после снятия ограничения Vercel) выполните:

```bash
cd /Users/dmitrii/Desktop/website
./deploy.sh
```

Или просто:
```bash
cd /Users/dmitrii/Desktop/website
git push
vercel --prod
```

### Вариант 2: Через Vercel Dashboard

1. Откройте https://vercel.com
2. Перейдите в проект `vintrusted`
3. На вкладке **Deployments** нажмите **Redeploy** на последнем деплое

## Что будет задеплоено:

✅ Исправление платежной формы (принудительная видимость)
✅ Автозаполнение VIN из кеша на мобилке
✅ Все предыдущие изменения (ClearVin API, success page и т.д.)

## Текущий статус:

- Все изменения закоммичены в GitHub
- Ожидается деплой после снятия ограничения Vercel (через ~11 часов)

