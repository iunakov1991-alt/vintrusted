# ⏰ КОГДА ОБНОВЛЯЕТСЯ СТАТУС

## 🔍 ТЕКУЩАЯ ЛОГИКА

### 1. Во время выполнения партии:
- `build_topics_batch_parallel.js` обновляет `tmp/batch-status.json` **локально** в GitHub Actions runner
- Обновляется после каждого батча страниц (строка 233)
- **НО:** Этот статус **НЕ отправляется** на Vercel во время выполнения

### 2. В конце выполнения:
- GitHub Actions шаг **"Update batch status via API"** (строка 146)
- Выполняется с `if: always()` - даже если партия упала
- Читает финальный статус из `tmp/batch-status.json`
- Отправляет POST на `/api/batch-status` → сохраняет в Upstash Redis

## ⏰ КОГДА ОБНОВИТСЯ

**Статус обновится:**
- ✅ **Только после завершения партии** (когда GitHub Actions workflow завершится)
- ❌ **НЕ обновляется во время выполнения** (только в конце)

## ⚠️ ПРОБЛЕМА

Партия уже завершилась (14:37:55Z), но статус не обновился в дашборде.

**Возможные причины:**
1. GitHub Actions не отправил статус (ошибка в шаге "Update batch status via API")
2. `BATCH_STATUS_TOKEN` не настроен в GitHub Secrets
3. Статус не сохранился в `tmp/batch-status.json`
4. Ошибка при отправке на Vercel API

## ✅ РЕШЕНИЕ

Нужно проверить логи GitHub Actions для последнего run, чтобы увидеть:
- Выполнился ли шаг "Update batch status via API"
- Какой HTTP код вернул API
- Есть ли ошибки

---

**Статус обновится после завершения следующей партии, если всё настроено правильно.**

**Для обновления во время выполнения нужно добавить периодическую отправку статуса.**

